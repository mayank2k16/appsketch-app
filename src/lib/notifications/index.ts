/**
 * Push notifications with Expo (Firebase FCM on Android).
 * Supports deep linking to specific screens when user taps a notification.
 */

export { setupNotifications, getExpoPushToken } from './setup';
