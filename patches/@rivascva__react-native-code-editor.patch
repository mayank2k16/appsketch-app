diff --git a/lib/commonjs/CodeEditor.js b/lib/commonjs/CodeEditor.js
index 0c37f187080dcbc681cc51cce3036521a3cdaef9..21d248afece251a14200c2c6069958702bbfe37a 100644
--- a/lib/commonjs/CodeEditor.js
+++ b/lib/commonjs/CodeEditor.js
@@ -116,8 +116,12 @@ const CodeEditor = props => {
   const handleScroll = e => {
     var _highlighterRef$curre;
 
-    // Match text input scroll with syntax highlighter scroll
-    const y = e.nativeEvent.contentOffset.y;
+    // Match text input scroll with syntax highlighter scroll.
+    // On web, react-native-web can fire onScroll with no contentOffset
+    // (e.g. a scrollend event), which crashed with "Cannot read
+    // properties of undefined (reading 'y')".
+    const y = e.nativeEvent.contentOffset?.y;
+    if (y === undefined) return;
     (_highlighterRef$curre = highlighterRef.current) === null || _highlighterRef$curre === void 0 ? void 0 : _highlighterRef$curre.scrollTo({
       y,
       animated: false
diff --git a/lib/commonjs/SyntaxHighlighter.js b/lib/commonjs/SyntaxHighlighter.js
index 94546acf7fb3c728967f2d0dafac3936d107121e..a4e4635bdc538319297588cc09916c8ab1d4efd9 100644
--- a/lib/commonjs/SyntaxHighlighter.js
+++ b/lib/commonjs/SyntaxHighlighter.js
@@ -45,8 +45,9 @@ const SyntaxHighlighter = props => {
     highlighterColor = undefined
   } = addedStyle || {}; // Only when line numbers are showing
 
-  const lineNumbersPadding = showLineNumbers ? 1.75 * fontSize : undefined;
-  const lineNumbersFontSize = 0.7 * fontSize; // Prevents the last line from clipping when scrolling
+  const lineNumberDigits = String((highlighterProps.children.match(/\n/g)?.length ?? 0) + 1).length;
+  const lineNumbersFontSize = 0.7 * fontSize;
+  const lineNumbersPadding = showLineNumbers ? Math.max(1.75 * fontSize, lineNumberDigits * lineNumbersFontSize + 10) : undefined; // Prevents the last line from clipping when scrolling
 
   highlighterProps.children += '\n\n';
 
@@ -87,17 +88,18 @@ const SyntaxHighlighter = props => {
             lineHeight: highlighterLineHeight,
             fontFamily,
             fontSize,
-            paddingLeft: lineNumbersPadding !== null && lineNumbersPadding !== void 0 ? lineNumbersPadding : padding
+            paddingLeft: key === '0' ? (lineNumbersPadding !== null && lineNumbersPadding !== void 0 ? lineNumbersPadding : padding) : 0
           }]
         }, renderNode(node.children, `${key}.${index}`));
 
         const lineNumberElement = key !== '0' || index >= nodes.length - 2 ? undefined : /*#__PURE__*/_react.default.createElement(_reactNative.Text, {
           key: `$line.${index}`,
+          numberOfLines: 1,
           style: {
             position: 'absolute',
             top: 5,
             bottom: 0,
-            paddingHorizontal: nodes.length - 2 < 100 ? 5 : 0,
+            paddingHorizontal: 4,
             textAlign: 'center',
             color: lineNumbersColor,
             fontFamily,
diff --git a/lib/module/CodeEditor.js b/lib/module/CodeEditor.js
index bec18b910ebf84f7fe7cf155aad4b4ca2bd6e21c..40f2229e97b990eac7ea222f89bdd26b6f820429 100644
--- a/lib/module/CodeEditor.js
+++ b/lib/module/CodeEditor.js
@@ -98,8 +98,12 @@ const CodeEditor = props => {
   const handleScroll = e => {
     var _highlighterRef$curre;
 
-    // Match text input scroll with syntax highlighter scroll
-    const y = e.nativeEvent.contentOffset.y;
+    // Match text input scroll with syntax highlighter scroll.
+    // On web, react-native-web can fire onScroll with no contentOffset
+    // (e.g. a scrollend event), which crashed with "Cannot read
+    // properties of undefined (reading 'y')".
+    const y = e.nativeEvent.contentOffset?.y;
+    if (y === undefined) return;
     (_highlighterRef$curre = highlighterRef.current) === null || _highlighterRef$curre === void 0 ? void 0 : _highlighterRef$curre.scrollTo({
       y,
       animated: false
diff --git a/lib/module/SyntaxHighlighter.js b/lib/module/SyntaxHighlighter.js
index f70d8a1efcb3deac1bccda18fdba322dafd9ccfd..dd4f33acdca5a70e1b950a254c324d731a900ea8 100644
--- a/lib/module/SyntaxHighlighter.js
+++ b/lib/module/SyntaxHighlighter.js
@@ -27,8 +27,9 @@ const SyntaxHighlighter = props => {
     highlighterColor = undefined
   } = addedStyle || {}; // Only when line numbers are showing
 
-  const lineNumbersPadding = showLineNumbers ? 1.75 * fontSize : undefined;
-  const lineNumbersFontSize = 0.7 * fontSize; // Prevents the last line from clipping when scrolling
+  const lineNumberDigits = String((highlighterProps.children.match(/\n/g)?.length ?? 0) + 1).length;
+  const lineNumbersFontSize = 0.7 * fontSize;
+  const lineNumbersPadding = showLineNumbers ? Math.max(1.75 * fontSize, lineNumberDigits * lineNumbersFontSize + 10) : undefined; // Prevents the last line from clipping when scrolling
 
   highlighterProps.children += '\n\n';
 
@@ -69,16 +70,17 @@ const SyntaxHighlighter = props => {
             lineHeight: highlighterLineHeight,
             fontFamily,
             fontSize,
-            paddingLeft: lineNumbersPadding !== null && lineNumbersPadding !== void 0 ? lineNumbersPadding : padding
+            paddingLeft: key === '0' ? (lineNumbersPadding !== null && lineNumbersPadding !== void 0 ? lineNumbersPadding : padding) : 0
           }]
         }, renderNode(node.children, `${key}.${index}`));
         const lineNumberElement = key !== '0' || index >= nodes.length - 2 ? undefined : /*#__PURE__*/React.createElement(Text, {
           key: `$line.${index}`,
+          numberOfLines: 1,
           style: {
             position: 'absolute',
             top: 5,
             bottom: 0,
-            paddingHorizontal: nodes.length - 2 < 100 ? 5 : 0,
+            paddingHorizontal: 4,
             textAlign: 'center',
             color: lineNumbersColor,
             fontFamily,
diff --git a/src/CodeEditor.tsx b/src/CodeEditor.tsx
index fc44a54942b332bdd941b2ef3fa8351ca2f45d36..0a4cf2dad744d3072d5a2d3a3d1ecd198f3d15bf 100644
--- a/src/CodeEditor.tsx
+++ b/src/CodeEditor.tsx
@@ -207,8 +207,12 @@ const CodeEditor = (props: PropsWithForwardRef): JSX.Element => {
     };
 
     const handleScroll = (e: NativeSyntheticEvent<TextInputScrollEventData>) => {
-        // Match text input scroll with syntax highlighter scroll
-        const y = e.nativeEvent.contentOffset.y;
+        // Match text input scroll with syntax highlighter scroll.
+        // On web, react-native-web can fire onScroll with no contentOffset
+        // (e.g. a scrollend event), which crashed with "Cannot read
+        // properties of undefined (reading 'y')".
+        const y = e.nativeEvent.contentOffset?.y;
+        if (y === undefined) return;
         highlighterRef.current?.scrollTo({ y, animated: false });
     };
 
diff --git a/src/SyntaxHighlighter.tsx b/src/SyntaxHighlighter.tsx
index fb5d53c21bf908d8249e1fec2e5cd85388f92f98..a68c78a92f026d3adf516f65a691eb22b58f8264 100644
--- a/src/SyntaxHighlighter.tsx
+++ b/src/SyntaxHighlighter.tsx
@@ -118,9 +118,17 @@ const SyntaxHighlighter = (props: PropsWithForwardRef): JSX.Element => {
         highlighterColor = undefined,
     } = addedStyle || {};
 
-    // Only when line numbers are showing
-    const lineNumbersPadding = showLineNumbers ? 1.75 * fontSize : undefined;
+    // Only when line numbers are showing.
+    // The gutter has to widen with the line count, or numbers with more
+    // digits than it was sized for (e.g. line 100+ in a file sized for a
+    // 2-digit gutter) wrap onto a second line instead of staying inline.
+    const lineNumberDigits = String(
+        (highlighterProps.children.match(/\n/g)?.length ?? 0) + 1
+    ).length;
     const lineNumbersFontSize = 0.7 * fontSize;
+    const lineNumbersPadding = showLineNumbers
+        ? Math.max(1.75 * fontSize, lineNumberDigits * lineNumbersFontSize + 10)
+        : undefined;
 
     // Prevents the last line from clipping when scrolling
     highlighterProps.children += '\n\n';
@@ -168,7 +176,13 @@ const SyntaxHighlighter = (props: PropsWithForwardRef): JSX.Element => {
                                 lineHeight: highlighterLineHeight,
                                 fontFamily,
                                 fontSize,
-                                paddingLeft: lineNumbersPadding ?? padding,
+                                // Only the line itself (key === '0', i.e. a direct child of
+                                // `rows`) needs left padding to clear the line-number gutter.
+                                // Applying it at every nesting level pads each individual
+                                // highlighted token (keywords, strings, types, ...) too,
+                                // which is what produced the stretched-out spacing between
+                                // tokens on a line.
+                                paddingLeft: key === '0' ? lineNumbersPadding ?? padding : 0,
                             },
                         ]}
                     >
@@ -180,11 +194,12 @@ const SyntaxHighlighter = (props: PropsWithForwardRef): JSX.Element => {
                     key !== '0' || index >= nodes.length - 2 ? undefined : (
                         <Text
                             key={`$line.${index}`}
+                            numberOfLines={1}
                             style={{
                                 position: 'absolute',
                                 top: 5,
                                 bottom: 0,
-                                paddingHorizontal: nodes.length - 2 < 100 ? 5 : 0,
+                                paddingHorizontal: 4,
                                 textAlign: 'center',
                                 color: lineNumbersColor,
                                 fontFamily,
